# Deprecated SQLite Legacy Scripts

These scripts were created during the initial SQLite-based architecture of the software.
The application has since been fully migrated to **MySQL** (`mysql2/promise`), with structured database migrations in `src/database/migrations/`.

These files are preserved here for historical reference only and should not be executed in production.
